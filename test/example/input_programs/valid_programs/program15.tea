## Sample Comment #
Recursive Function
still commenting ##

a := 100
b := 50
c := 150

funcA := (a, b, c) ->
    if a <= b or c <= a:
        out(a)
    else:
        c = c--
        funcA(a, b, c)
    end
end