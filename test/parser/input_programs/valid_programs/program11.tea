(1*8)
f := (a, b, c) ->
  x := a + 100
  a = b
  return x
end

g := (first_arg, another_arg) ->
  out('hi')
  string := "here is a super cool string thing"
  return f(string, <first_arg, 1>, <another_arg>)
end

(anonymous_functions) -> are('cool')
